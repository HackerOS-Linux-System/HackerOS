# Blue Environment log directory (world-writable + sticky, like /tmp), so the
# shell can log to /var/log/Blue-Environment instead of falling back to
# ~/.cache/logs. Created at every boot by systemd-tmpfiles.
d /var/log/Blue-Environment 1777 root root -
